import { Dimensions } from 'react-native'

const WIDTH_SCREEN = Dimensions.get('window').width
const HEIGHT_SCREEN = Dimensions.get('window').height

const ICON_SIZE_DEFAULT = 24
const ICON_LOGO_SIZE = 40

export const sizes = {
  WIDTH_SCREEN,
  HEIGHT_SCREEN,
  ICON_SIZE_DEFAULT,
  ICON_LOGO_SIZE,
}
